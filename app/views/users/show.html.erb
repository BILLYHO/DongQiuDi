<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= image_tag  @user.weibo_avatar_large ? @user.weibo_avatar_large : gravatar_for(@user) %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
</div>